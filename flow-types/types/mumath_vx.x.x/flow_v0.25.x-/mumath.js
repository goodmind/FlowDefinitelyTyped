declare module "mumath" {
  import typeof * as clamp from "./clamp";

  import typeof * as closest from "./closest";

  import typeof * as isMultiple from "./isMultiple";

  import typeof * as len from "./len";

  import typeof * as lerp from "./lerp";

  import typeof * as mod from "./mod";

  import typeof * as order from "./order";

  import typeof * as precision from "./precision";

  import typeof * as round from "./round";

  import typeof * as scale from "./scale";

  import typeof * as within from "./within";

  declare export {
    clamp,
    closest,
    isMultiple,
    len,
    lerp,
    mod,
    order,
    precision,
    round,
    scale,
    within
  };
}
