declare module 'detect-touch-events' {
        declare interface detectTouchEvents {
hasApi: boolean,
maxTouchPoints: number,
update(): void
} 
	declare var detectTouchEvents: detectTouchEvents;
	declare export default typeof detectTouchEvents

    }
