declare module "repeat-element" {
  /**
   * Create an array by repeating the given value n times.
   */
  declare function repeat(element: mixed, num: number): mixed[];

  declare export default typeof repeat;
}
